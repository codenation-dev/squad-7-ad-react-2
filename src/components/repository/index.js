import React from 'react';

// import { Container } from './styles';

const Repository = ({ name, avatar }) => (
  <div>
    <h3>{name}</h3>
    <h3>{avatar}</h3>
  </div>
);

export default Repository;
